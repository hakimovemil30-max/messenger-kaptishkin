# === Зависимости Node.js / npm / Yarn ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# === Локальные настройки IDE и редакторов ===
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# === Временные папки сборщиков (если будете использовать Vite/Webpack/Rollup) ===
dist/
build/
out/
.parcel-cache/
.next/
.cache/
.env.local
.env.development.local
.env.test.local
.env.production.local

# === Логи сервера и отладка ===
logs
*.log
debug.log
coverage/
.nyc_output/

# === Файлы сокетов Unix ===
.sock

# === Специфика фронтенда мессенджера ===
uploads/          # если сервер будет сохранять картинки локально
storage/          # альтернативное имя для хранилища файлов
tmp/
temp/
*.db              # возможные SQLite базы данных чатов
*.sqlite
*.sqlite3

# === Секреты (на случай добавления бэкенда .env) ===
.env
.env.example
secrets.yml
id_rsa
